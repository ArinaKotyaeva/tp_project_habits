# 🚀 Как запустить проект "Учёт привычек"

## Быстрый запуск (Windows)

### Вариант 1: Использовать готовые скрипты

1. **Запустить Backend:**
   - Дважды кликните на `start_backend.bat` в корне проекта
   - Или в терминале: `start_backend.bat`

2. **Запустить Frontend:**
   - Откройте **новый терминал**
   - Дважды кликните на `start_frontend.bat` в корне проекта
   - Или в терминале: `start_frontend.bat`

3. **Открыть в браузере:**
   - http://localhost:3000

---

## Вариант 2: Ручной запуск

### Шаг 1: Запустить Backend

Откройте **первый терминал**:

```bash
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python run.py
```

Должно появиться:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

**⚠️ Оставьте этот терминал открытым!**

### Шаг 2: Запустить Frontend

Откройте **второй терминал** (новый):

```bash
cd frontend
npm install
npm run dev
```

Должно появиться:
```
➜  Local:   http://localhost:3000/
```

### Шаг 3: Открыть в браузере

Откройте http://localhost:3000

---

## ✅ Проверка работы

### Backend работает, если:
- В терминале видно: `INFO:     Uvicorn running on http://0.0.0.0:8000`
- В браузере http://localhost:8000 показывает JSON с информацией об API
- http://localhost:8000/status показывает красивую страницу статуса
- http://localhost:8000/docs открывает документацию API

### Frontend работает, если:
- В терминале видно: `➜  Local:   http://localhost:3000/`
- В браузере http://localhost:3000 показывает интерфейс приложения
- Можно добавлять, редактировать и удалять привычки

---

## 🔧 Если что-то не работает

### Backend не запускается:
1. Проверьте, что Python 3.11+ установлен: `python --version`
2. Убедитесь, что вы в папке `backend`
3. Проверьте, что все зависимости установлены: `pip install -r requirements.txt`

### Frontend не запускается:
1. Проверьте, что Node.js установлен: `node --version`
2. Убедитесь, что вы в папке `frontend`
3. Установите зависимости: `npm install`

### Белый экран в браузере:
1. Откройте консоль браузера (F12 → Console)
2. Проверьте ошибки
3. Убедитесь, что **оба** сервера запущены:
   - Backend на порту 8000 ✅
   - Frontend на порту 3000 ✅

---

## 📝 Полезные команды

### Остановить серверы:
- В терминале нажмите `Ctrl+C`

### Перезапустить:
- Остановите сервер (Ctrl+C)
- Запустите снова

### Проверить, что порты свободны:
- Backend использует порт **8000**
- Frontend использует порт **3000**
- Если порты заняты, закройте другие приложения

---

## 🎯 Структура запуска

```
Терминал 1 (Backend):
  cd backend
  python run.py
  → Запущен на http://localhost:8000

Терминал 2 (Frontend):
  cd frontend
  npm run dev
  → Запущен на http://localhost:3000

Браузер:
  → Открыть http://localhost:3000
```

---

## 💡 Советы

1. **Всегда запускайте оба сервера** - Backend и Frontend должны работать одновременно
2. **Не закрывайте терминалы** - серверы должны работать постоянно
3. **Используйте два терминала** - один для backend, другой для frontend
4. **Проверяйте консоль браузера** (F12) если что-то не работает

---

Удачной работы! 🎉

