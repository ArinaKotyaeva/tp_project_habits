# 🔧 Исправление проблемы с Git

## Проблема
Git репозиторий был инициализирован в неправильной директории (в домашней папке пользователя), поэтому он пытается добавить все файлы.

## Решение

### Шаг 1: Удалите lock файл (если есть)

```bash
# Удалите lock файл
Remove-Item "C:\Users\Арина Котяева\.git\index.lock" -ErrorAction SilentlyContinue
```

### Шаг 2: Убедитесь, что вы в правильной директории

```bash
cd "C:\Users\Арина Котяева\Desktop\MISIS\tp_project"
```

### Шаг 3: Проверьте, есть ли уже .git в проекте

```bash
# Если есть .git в корне проекта - это правильно
# Если нет - нужно инициализировать
```

### Шаг 4: Если .git находится в неправильном месте

```bash
# Удалите старый .git (если он в домашней папке)
# НЕ УДАЛЯЙТЕ .git из папки проекта!

# Инициализируйте git в правильной директории
git init
```

### Шаг 5: Добавьте remote

```bash
git remote add origin https://github.com/ArinaKotyaeva/tp_project_habits.git
```

### Шаг 6: Добавьте файлы проекта

```bash
git add .
```

### Шаг 7: Проверьте статус

```bash
git status
```

Должны видеть только файлы из папки `tp_project`, НЕ файлы из Downloads/Documents!

### Шаг 8: Commit и Push

```bash
git commit -m "Initial commit: Habits Tracker application"
git push -u origin main
```

